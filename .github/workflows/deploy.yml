name: Deploy Ludo

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: git -C /srv/ludo pull origin main

      - name: Rebuild and restart container
        run: cd /srv/ludo && docker compose up --build -d
